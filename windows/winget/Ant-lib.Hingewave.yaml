# Version manifest as submitted to microsoft/winget-pkgs (PR #435771), schema 1.12.0.
# At each release: bump PackageVersion and the URLs, refresh InstallerSha256 from the release
# SHA256SUMS-windows.txt, then open a new PR under manifests/a/Ant-lib/Hingewave/<version>/.
# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.12.0.schema.json

PackageIdentifier: Ant-lib.Hingewave
PackageVersion: 0.2.3
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
